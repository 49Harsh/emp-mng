# Root-level env example — copy to .env if running both services together
# See backend/.env.example and frontend/.env.example for per-service variables

# Docker Compose overrides
JWT_SECRET=change_this_to_a_strong_random_secret
JWT_REFRESH_SECRET=change_this_to_another_strong_secret
